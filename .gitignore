# Dependencies and build output
node_modules/
dist/

# NEVER commit these — they contain or produce API keys
bundled.dat
*-dad.exe
settings.json

# Local scratch
*.log
.DS_Store
Thumbs.db

# Claude Code tooling is local to whoever uses it — other contributors may
# use a different assistant or none at all.
.claude/
